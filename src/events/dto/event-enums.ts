/**
 * Shared event enums
 * These match the Prisma schema enums
 */
export enum EventRole {
  HOST = 'HOST',
  GIFTER = 'GIFTER',
}

export enum EventVisibility {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
}
