/**
 * Shared event enums
 * These match the Prisma schema enums
 */
export enum EventRole {
  ATTENDEE = 'ATTENDEE',
  PERFORMER = 'PERFORMER',
  CELEBRANT = 'CELEBRANT',
}

export enum EventVisibility {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
}

